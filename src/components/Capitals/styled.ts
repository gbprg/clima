import { styled } from "styled-components";

export const Container = styled.div`
  .title {
    font-size: 1.5rem;
  }

  .content {
    display: flex;
    gap: 1rem;
  }

  .degrees {
    font-weight: 300;
    display: flex;
    flex-direction: column;
    text-align: center;
    gap: 10px;
    color: #000;
    width: 2rem;
  }
`;
